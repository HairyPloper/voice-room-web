# voice_room_web

A small web project — initial repository files created by assistant.

## Quick start

1. (Optional) Add a remote: `git remote add origin <URL>`
2. Push: `git push -u origin main`

